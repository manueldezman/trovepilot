"use client";

import { Timeline } from "@/components/Timeline";

export default function TimelinePage() {
  return (
    <main style={{ display: "grid", gap: 14, maxWidth: 1100, margin: "0 auto" }}>
      <Timeline />
    </main>
  );
}
